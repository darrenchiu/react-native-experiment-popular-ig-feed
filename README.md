# react-native-experiment-popular-ig-feed
An experiment to try out react native

# To Setup
You will need to add a config.js file at project root with the following line:
export const igToken = Your Instagram Token;
